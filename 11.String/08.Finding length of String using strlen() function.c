/*
 Why determine string length?
 How we use string length?
 what is the name of the function to determine string length?
*/

#include<stdio.h>
int main()
{
    char ch[]= {"EmranHossen"};
    int len=strlen(ch);

    printf(" The Length is:%d",len);

    getch ();
}
